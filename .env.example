# Example environment configuration for go-crud-server
# Copy this file to `.env` and adjust values for your environment.

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# DSN for PostgreSQL used by GORM.
DSN="host=localhost user=postgres password=postgres dbname=go_crud_server port=5432 sslmode=disable"

# ---------------------------------------------------------------------------
# Auth / JWT
# ---------------------------------------------------------------------------
# Secret key used to sign JWT tokens.
# Use a long, random string in production.
SECRET="super-secret-development-key-change-me"